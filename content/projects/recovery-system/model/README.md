# Recovery Board STEP Export

Place the Altium-exported 3D board model here:

```text
recovery-board.step
```

The portfolio viewer will automatically load the file referenced by `model3d.stepFile` in `project.json`.
